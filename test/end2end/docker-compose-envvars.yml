version: '2'
services:
  dct_s1:
    image: alpine
    ports:
      - '3001'
    command: 'tail -f ${FILE_TO_TAIL}'
  print_env:
    image: alpine
    ports:
      - '3001'
    command: 'env'
    environment:
      - P2=${PATH}
